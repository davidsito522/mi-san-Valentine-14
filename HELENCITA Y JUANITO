<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Te amo mucho</title>
  <style>
    /* Estilos generales */
    body {
      background: linear-gradient(to bottom, #ffebf0, #ff6b81);
      height: 100vh;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
      font-family: 'Arial', sans-serif;
      text-align: center;
      color: white;
    }

    h1 {
      font-size: 2.8em;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
    }

    button {
      background-color: #f06292;
      border: none;
      color: white;
      font-size: 1.2em;
      padding: 12px 30px;
      margin-top: 30px;
      cursor: pointer;
      border-radius: 20px;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
      transition: transform 0.2s, background-color 0.3s ease;
    }

    button:hover {
      background-color: #e8435c;
      transform: scale(1.1);
    }

    .tulip {
      position: absolute;
      bottom: -10vh;
      animation: sway 6s infinite ease-in-out;
    }

    .tulip::before,
    .tulip::after {
      content: '';
      position: absolute;
      width: 10px;
      height: 50px;
      background-color: #4caf50;
      border-radius: 5px;
      transform-origin: bottom;
    }

    .tulip::before {
      left: -8px;
      transform: rotate(-30deg);
    }

    .tulip::after {
      right: -8px;
      transform: rotate(30deg);
    }

    .flower {
      position: absolute;
      background-color: #ff6f91;
      width: 20px;
      height: 20px;
      border-radius: 50% 50% 0 0;
      transform-origin: bottom;
    }

    .flower.left {
      left: -12px;
      transform: rotate(135deg);
    }

    .flower.right {
      right: -12px;
      transform: rotate(-135deg);
    }

    /* Animaciones */
    @keyframes sway {
      0%, 100% {
        transform: translateY(5vh) rotate(0deg);
      }
      50% {
        transform: translateY(-3vh) rotate(2deg);
      }
    }

    .tulip:nth-child(1) { left: 10%; animation-duration: 7s; }
    .tulip:nth-child(2) { left: 30%; animation-duration: 8s; }
    .tulip:nth-child(3) { left: 50%; animation-duration: 6.5s; }
    .tulip:nth-child(4) { left: 70%; animation-duration: 7.5s; }
    .tulip:nth-child(5) { left: 90%; animation-duration: 8s; }

  </style>

  <script>
    function changeMessage() {
      const message = document.querySelector("h1");
      message.innerText = "¿Quieres ser mi San Valentín? 🌷❤️";
      document.querySelector("button").style.display = "none";
    }
  </script>
</head>
<body>
  <div>
    <h1>Te amo mucho, mi amor 💕</h1>
    <button onclick="changeMessage()">Click aquí</button>
  </div>

  <!-- Tulipanes generados con CSS -->
  <div class="tulip">
    <div class="flower left"></div>
    <div class="flower right"></div>
  </div>
  <div class="tulip">
    <div class="flower left"></div>
    <div class="flower right"></div>
  </div>
  <div class="tulip">
    <div class="flower left"></div>
    <div class="flower right"></div>
  </div>
  <div class="tulip">
    <div class="flower left"></div>
    <div class="flower right"></div>
  </div>
  <div class="tulip">
    <div class="flower left"></div>
    <div class="flower right"></div>
  </div>
</body>
</html>
